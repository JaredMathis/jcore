import { path_is } from '../path/is.mjs';
import { error } from '../error.mjs';
import { arguments_assert } from '../arguments/assert.mjs';
export function file_is(file_path) {
    arguments_assert(arguments, [path_is]);
    error('todo: file_is');
}