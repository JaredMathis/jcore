import { list_is } from './is.mjs';
import { error } from '../error.mjs';
import { arguments_assert } from '../arguments/assert.mjs';
export function list_consume(list) {
    arguments_assert(arguments, [list_is]);
    error('todo: list_consume');
}