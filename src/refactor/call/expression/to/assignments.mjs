import { error } from '../../../../error.mjs';
import { arguments_assert } from '../../../../arguments/assert.mjs';
export function refactor_call_expression_to_assignments() {
    arguments_assert(arguments, []);
    error('todo: refactor_call_expression_to_assignments');
}