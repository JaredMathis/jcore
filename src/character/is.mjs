import { error } from '../error.mjs';
import { arguments_assert } from '../arguments/assert.mjs';
export function character_is() {
    arguments_assert(arguments, []);
    error('todo: character_is');
}