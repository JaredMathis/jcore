import { arguments_assert } from '../../arguments/assert.mjs';
import { metadata } from '../../metadata.mjs';
export function js_suffix_get() {
    arguments_assert(arguments, []);
    return 'get';
    metadata([]);
}